<div class="page-header" id="category_header">
  <h3><%= (link_to("All Posts", posts_path) + " &raquo; <small>#{@category.name}".html_safe)%></small></h3>
</div>

<% @category.posts.each do |post| %>
  <div id="category_feed">
    <%= render "posts/post", post: post %>
  </div>
<% end %>  